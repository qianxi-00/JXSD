# -*- coding: utf-8 -*-
"""自媒体 Agent —— Streamlit 页面包

课案出处：自媒体课案 → 各模块的 Streamlit 页面

每个文件一个页面函数（``show_xxx()``），由 ``main.py`` 的侧边栏路由调用。
页面只做两件事：收集输入、渲染 ``workflows/`` 返回的结果 ——
**不写业务逻辑，也不直接调 LLM**，这样工作流可以脱离界面单独测试。

统一约定
    · 结果一律先存 ``st.session_state`` 再渲染。
      Streamlit 的下载按钮会触发整页 rerun，不缓存结果页面会变空白 ——
      这是课案反复强调的坑（见各页面注释）。
    · 每次操作往 ``st.session_state.history`` 追加一条，首页展示最近 10 条。
    · 页面顶部标题用 emoji + 中文名，与课案一致。

与课案的落地差异
    | 项 | 课案原文 | 本实现 | 原因 |
    |---|---|---|---|
    | 本文件 | 课案的项目结构里**没有** ``views/__init__.py``（只列了 7 个页面文件） | 新增包入口，**只放本说明、不放任何代码** | 把「页面不写业务逻辑」「结果先落 ``session_state``」这类每个页面都要遵守的约定收在一处，新增页面照抄即可，不必回头翻课案 |
    | 页面函数签名 | ``def show_home():`` —— 无返回注解 | 7 个页面统一 ``def show_xxx() -> None:`` | 与全仓风格一致；``main.py`` 的路由表只是「取出即调用」，不消费返回值 |
    | 绝对路径 | 无 | 无 | —— |

踩过的坑
    · **包入口会在任何一个 ``views.xxx`` 被 import 时先执行** —— ``verify_all.py``
      第 2 层的 ``import views.home`` 同样会连带跑到这里。所以本文件只写文档：
      一旦往这里放公共 import 或建客户端的代码，第 2 层就不再是「零副作用的语法检查」。
    · 「结果先落 ``st.session_state``」不是代码风格问题：``st.download_button``
      一点就触发整页 rerun，只存在局部变量里的结果会被清空，用户看到的是「结果丢了」。
    · 页面数量以 ``main.py`` 的 ``PAGES`` / ``ROUTES`` 为准（各 7 项）：
      课案正文的目录树写「views/ # 6 个 Streamlit 页面」，紧接着却列了 7 个文件；
      下一段「workflows/ # 6 个 LangGraph 工作流」下面同样列了 8 行。
"""
