# ============================================================
# .dockerignore —— 构建镜像时要排除的文件（放在构建上下文根目录）
# ============================================================
# 作用：docker build 会先把"构建上下文"整个发给 Docker 守护进程，
#       排除掉不需要的文件能显著加快构建、减小镜像体积、避免泄露敏感信息。
#
# 放置位置：本文件应与 docker build 的 context 目录配套使用。
#           docker-compose.yml 里 context 是 Back_End 的上级目录（仓库根），
#           因此实际使用时把它复制到仓库根目录并在其中加上子项目路径即可。
# ============================================================

# ---------- 版本控制与 CI ----------
.git
.gitignore
.github
.gitlab-ci.yml

# ---------- 虚拟环境（最重要的一条！） ----------
# 一旦被打进上下文，镜像会凭空多出几百 MB
.venv
venv
env
ENV
.uv-cache

# ---------- Python 缓存 ----------
__pycache__
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info
.eggs

# ---------- 测试与检查工具产物 ----------
.pytest_cache
.ruff_cache
.mypy_cache
.coverage
htmlcov
.tox

# ---------- IDE 与操作系统 ----------
.idea
.vscode
*.swp
.DS_Store
Thumbs.db

# ---------- 敏感信息（绝对不能进镜像！） ----------
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ---------- 运行时数据（镜像里不要带数据库文件与日志） ----------
Back_End/data
**/data/*.db
**/data/*.log
**/data/logs
logs
*.sqlite3

# ---------- 与本镜像无关的子项目（多项目仓库里很关键） ----------
Machine_Learning
Deep_Learning
Front_End
RAG
Agent
Crawler
Data_Analysis
Data_Structure
Py_Advanced
.course_extract
.files
.jbeval

# ---------- 文档与笔记 ----------
*.md
!README.md
*.ipynb
.ipynb_checkpoints
