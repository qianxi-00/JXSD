# VERIFY_REPORT —— `verify_all.py` 完整实测报告

> 本文件由实现工程师在交付前**真实执行**并原样记录，**没有手工修饰任何数值**。
> 本报告是**移除临时环境兼容补丁之后的最终回归结果**：`kiwisolver` 已修复，全部脚本使用
> 标准 `import matplotlib.pyplot as plt`，不再依赖任何补丁。
> 生成方式：先运行 `verify_all.py` 把完整输出重定向到临时文件，再把临时文件内容原样嵌入第六节，
> 并删除临时文件。下方「耗时速览」表格中的每一行都是从该输出里解析出来的，与第六节完全一致。

## 一、执行环境

| 项目 | 值 |
|---|---|
| 工作区根目录 | `F:\ProGram\Python_Base` |
| 交付目录 | `F:\ProGram\Python_Base\Deep_Learning` |
| 解释器 | `F:\ProGram\Python_Base\.venv\Scripts\python.exe` |
| Python 版本 | 3.12.12 |
| PyTorch | 2.14.0+cpu（`torch.cuda.is_available() == False`，全程纯 CPU 运行） |
| kiwisolver | **1.5.0（已修复：从官方 wheel 补回缺失的 `__init__.py`）** |
| matplotlib | 3.11.1（Agg 无界面后端；脚本内绝不调用 `plt.show()`） |
| 环境补丁 | **无** —— 临时兼容模块已删除，20 个脚本均已移除兼容导入与 `sys.path` 注入 |
| 随机种子 | `torch.manual_seed(42)` / `np.random.seed(42)` |
| 子进程编码 | `PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1`（由 `verify_all.py` 注入，规避中文 Windows 的 GBK 陷阱） |
| 线程限制 | `OMP_NUM_THREADS=4`、`MKL_NUM_THREADS=4` |
| 单脚本超时 | 300 秒（实际最慢 22.83 秒） |

## 二、实际执行的命令

### 2.1 一键自检（本报告的数据来源）

```powershell
`='utf-8'
& 'F:\ProGram\Python_Base\.venv\Scripts\python.exe' 'F:\ProGram\Python_Base\Deep_Learning\verify_all.py'
```

`verify_all.py` 内部对每个脚本执行的就是：

```python
subprocess.run(
    [sys.executable, str(script)],
    capture_output=True,
    text=True,
    encoding="utf-8",
    errors="replace",
    timeout=300,
    env=env,           # PYTHONIOENCODING=utf-8 / PYTHONUTF8=1 / OMP_NUM_THREADS=4 / MKL_NUM_THREADS=4
    cwd=str(ROOT),     # 故意用 Deep_Learning 作为工作目录，验证脚本不依赖当前工作目录
)
```

### 2.2 环境健康自检（确认不再需要任何补丁）

```powershell
`='utf-8'
& 'F:\ProGram\Python_Base\.venv\Scripts\python.exe' -c "import kiwisolver, matplotlib; print(kiwisolver.__version__, matplotlib.__version__); import matplotlib.pyplot as plt; import matplotlib._layoutgrid; print('pyplot + _layoutgrid OK')"
```

实测输出：

```text
1.5.0 3.11.1
pyplot + _layoutgrid OK
```

### 2.3 单独复跑某个脚本并计时（示例）

```powershell
`='utf-8'
& 'F:\ProGram\Python_Base\.venv\Scripts\python.exe' -c "import subprocess,sys,time;t=time.perf_counter();r=subprocess.run([sys.executable,r'F:\ProGram\Python_Base\Deep_Learning\02_网络架构\06_Transformer完整实现.py'],capture_output=True,text=True,encoding='utf-8');print('rc',r.returncode,'%.2fs'%(time.perf_counter()-t));print(r.stdout[-1500:]);print('STDERR:',r.stderr[-2000:])"
```

## 三、耗时速览（本次实测，由 `verify_all.py` 汇总）

| 脚本 | 结果 | 耗时 |
|---|---|---|
| `04_训练技巧\01_过拟合与欠拟合演示.py` | OK | 22.83s |
| `03_训练组件\06_正则化.py` | OK | 13.19s |
| `01_PyTorch基础\03_数据集与DataLoader.py` | OK | 10.00s |
| `04_训练技巧\02_梯度爆炸与梯度消失演示.py` | OK | 9.84s |
| `03_训练组件\07_学习率调度.py` | OK | 9.05s |
| `03_训练组件\03_优化器全家桶.py` | OK | 7.24s |
| `03_训练组件\04_评估指标.py` | OK | 5.64s |
| `03_训练组件\01_激活函数.py` | OK | 5.60s |
| `03_训练组件\05_参数初始化.py` | OK | 5.50s |
| `02_网络架构\02_DNN前向传播与随机失活.py` | OK | 5.46s |
| `02_网络架构\06_Transformer完整实现.py` | OK | 4.59s |
| `02_网络架构\04_RNN_LSTM_GRU.py` | OK | 4.24s |
| `01_PyTorch基础\04_权重存储与精度类型.py` | OK | 4.20s |
| `02_网络架构\01_Embedding词嵌入.py` | OK | 3.83s |
| `03_训练组件\02_损失函数.py` | OK | 3.72s |
| `03_训练组件\08_模型保存与推理.py` | OK | 3.60s |
| `01_PyTorch基础\02_自动微分.py` | OK | 3.36s |
| `02_网络架构\03_CNN卷积与池化.py` | OK | 3.02s |
| `02_网络架构\05_Attention与自注意力.py` | OK | 2.63s |
| `01_PyTorch基础\01_张量.py` | OK | 2.57s |

- **合计耗时：130.12 秒**（20 个脚本，每次都包含进程启动 + `import torch` 的固定开销约 1.5~2.5 秒）。
- **最慢脚本 22.83 秒**，最快 2.57 秒；**全部满足「单脚本 < 40 秒」的约束**。
- 最慢的 `04_训练技巧\01_过拟合与欠拟合演示.py` 需要在一份数据上训练 5 组模型（小模型 / 大模型 /
  +Dropout / +WeightDecay / +两者）各 150 epoch 并画决策边界网格，换来的是
  「过拟合 → 正则化 → 泛化差距缩小」这条完整证据链。

## 四、最终汇总（原样粘贴）

```text
共 20 个脚本，成功 20 个，失败 0 个
```

## 五、交付前检查结论

| 检查项 | 结论 |
|---|---|
| 全部脚本返回码 | **20/20 均为 0** |
| `Traceback` | **0 处**（对整份报告做过全文检索） |
| `Error` / `Exception` / `Warning` 字样 | **0 处** |
| 子进程 stderr | **全部为空** —— 报告中不存在任何「标准错误」小节，说明**没有字体告警**（matplotlib 的 CJK glyph 警告会写 stderr） |
| 中文渲染 | 正常：中文标题 / 图例 / 轴标签无方框乱码（已抽查生成的 PNG） |
| 兼容补丁引用 | **0 处**（全目录含 `.md` / `.pyc` 检索无命中） |
| 单脚本耗时 | 全部 < 40 秒（最慢 22.83 秒） |
| 输出文件 | 39 张 PNG + 4 个 `.pt` 均被真实重新生成（`output/` 最新写入时间已在本次运行中刷新） |
| 目录边界 | 全部文件都在 `Deep_Learning/` 内；未改动工作区其它文件 |

## 六、`verify_all.py` 的完整输出（原样粘贴）

```text
==============================================================================
Deep_Learning 目录脚本一键自检（verify_all.py）
==============================================================================
Deep_Learning 根目录 : F:\ProGram\Python_Base\Deep_Learning
使用解释器           : F:\ProGram\Python_Base\.venv\Scripts\python.exe
Python 版本          : 3.12.12
待运行脚本数量       : 20
单脚本超时上限       : 300 秒
输出目录             : F:\ProGram\Python_Base\Deep_Learning\output
==============================================================================

------------------------------------------------------------------------------
[1/20] 运行：01_PyTorch基础\01_张量.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：2.57 秒
--- 最后 15 行输出 ---
    
    ==============================================================================
    小结
    ==============================================================================
    · 创建：torch.tensor(列表) / torch.from_numpy(共享内存) / ones·zeros·randn·arange·linspace·eye
    · 默认 dtype：整数→int64，浮点→float32（由 torch.get_default_dtype() 决定）
    · 运算：元素级用 + * - / **（形状不变），矩阵乘法用 @（(m,n)@(n,p)→(m,p)）
    · 广播：从右往左对齐，维度相等或其中之一为 1 才能广播；(3,1)+(1,4)→(3,4)
    · 变形：reshape 宽容（必要时拷贝），view 要求内存连续，否则先 .contiguous()
    · 维度：unsqueeze 补 1 维、squeeze 删长度 1 的维、transpose 换两轴、permute 换多轴
    · 索引：切片保留维度，整数/布尔索引会降维或压平；index_select、gather 按索引取值
    · 拼接：cat 沿已有维（维度数不变），stack 新建维（维度数 +1）
    · 设备：torch.cuda.is_available()=False（CPU 版），写 .to(device) 保持设备无关
    ==============================================================================
    脚本执行完毕，退出码 0。

------------------------------------------------------------------------------
[2/20] 运行：01_PyTorch基础\02_自动微分.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：3.36 秒
--- 最后 15 行输出 ---
    ==============================================================================
    小结
    ==============================================================================
    · requires_grad=True 的叶子张量才会被记录进计算图，backward() 后梯度写进 .grad
    · backward() 只能从标量出发；非标量要用 grad_outputs 指定上游权重
    · grad_fn 记录「我由哪个运算产生」，next_functions 指向更上游，可据此打印整张图
    · .grad 是累加的：不清零就翻倍 → 训练循环第一行必须 optimizer.zero_grad()
    · retain_graph=True 保留计算图以便再次反传；默认反传后图被释放
    · detach() 剪断单个张量（共享内存，不建图）；no_grad() 关闭一段代码的建图
    · torch.autograd.grad 只返回梯度、不写 .grad；支持多输入 / grad_outputs / allow_unused
    · create_graph=True 让梯度本身可导 → 可求二阶导、高阶导
    · 手写 numpy 反向传播与 autograd 的梯度最大误差 = 1.57e-16（< 1e-6，验证正确性）
    · 课案三行：optimizer.zero_grad() → loss.backward() → optimizer.step()
    ==============================================================================
    脚本执行完毕，退出码 0。

------------------------------------------------------------------------------
[3/20] 运行：01_PyTorch基础\03_数据集与DataLoader.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：10.00 秒
--- 最后 15 行输出 ---
    小结
    ==============================================================================
    · Dataset 管「单条样本怎么读」：必须实现 __init__ / __len__ / __getitem__
    · DataLoader 管「怎么组成 batch」：batch_size / shuffle / num_workers / collate_fn / pin_memory
    · 100 条样本 batch_size=16：drop_last=False → 7 个 batch（6×16 + 4）；True → 6 个 batch
    · shuffle=True 让每个 batch 的类别分布接近总体，训练更稳；验证集用 shuffle=False
    · Windows 上 num_workers>0 需要 __main__ 保护，且要被 pickle 的类必须定义在模块级
    · 变长序列必须自定义 collate_fn 做 padding，并同时产出 mask，否则 padding 会污染 loss
    · TensorDataset(X, y) 按第 0 维对齐，两行代码就能跑通训练循环
    · len(dataloader) = ceil(n/batch_size)（drop_last=False）或 n//batch_size（drop_last=True）
    · random_split / Subset 切分训练集与验证集，都是零拷贝的视图
    · 手写 MyDataLoader = 打乱下标 + 按 batch_size 切片 + 列表推导 + zip(*samples) 转置
    · 训练循环五行：zero_grad → forward → loss → backward → step
    ==============================================================================
    脚本执行完毕，退出码 0。

------------------------------------------------------------------------------
[4/20] 运行：01_PyTorch基础\04_权重存储与精度类型.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：4.20 秒
--- 最后 15 行输出 ---
    ==============================================================================
    小结
    ==============================================================================
    · state_dict 是有序字典，只含 parameters + buffers，不含网络结构；值和模型共享内存
    · load_state_dict 是原地拷贝；strict=True 全有或全无，strict=False 返回 missing/unexpected_keys
    · 形状不匹配 strict 也救不了；加载到新结构时 strict=False 是迁移学习的标准写法
    · 保存一律用 torch.save(model.state_dict())；torch.save(model) 依赖类定义且有 pickle 安全风险
    · 完整 checkpoint 必须带上 optimizer / scheduler / epoch / best_metric / config，才能断点续训
    · 格式：.pt/.pth/.bin（PyTorch）、Safetensors（安全）、GGUF（CPU 量化）、ONNX（跨框架）、TF（需转换）
    · 位数结构：FP32=1+8+23、FP16=1+5+10、BF16=1+8+7、INT8=8 位整数（需 scale/zero_point）
    · FP16 范围 ±[6.1e-5, 6.55e4]：1e-8 及更小下溢成 0、70000 溢出成 inf，所以需要 loss scaling
    · BF16 范围与 FP32 相同但尾数只有 7 位，精度比 FP16 差约 8 倍，好在范围够大不用缩放
    · 混合精度：低精度算、FP32 累加（归约/loss 保留高精度），autocast 在 CPU 上支持有限需 try/except
    ==============================================================================
    脚本执行完毕，退出码 0。

------------------------------------------------------------------------------
[5/20] 运行：02_网络架构\01_Embedding词嵌入.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：3.83 秒
--- 最后 15 行输出 ---
    九、本节小结
    ==========================================================================
    1. Embedding 把离散 token id 映射为 d_model 维稠密向量，解决了 One-Hot 的
       维度爆炸（50000 维稀疏）与正交无语义（猫/狗点积为 0）两大问题。
    2. 本质是查表：W 形状 (vocab_size, d_model)，第 i 行就是第 i 个词的向量；
       已用 F.one_hot(ids, V).float() @ W 与 nn.Embedding 的 0 误差验证。
    3. 形状规律：(batch, seq_len) -> (batch, seq_len, d_model)。
    4. padding_idx 让补齐位向量恒 0 且梯度恒 0；max_norm 限制范数上界；
       EmbeddingBag 直接输出聚合成的一袋词定长表示。
    5. 在 8 词人造共现任务上训练 100 步后，同类词余弦相似度明显高于跨类词，
       最近邻也几乎总是同类词，说明『相似的词向量也相似』是任务驱动的
       梯度下降学出来的，而不是人工写死的。
    
    脚本总耗时：1.39 秒
    脚本正常结束。

------------------------------------------------------------------------------
[6/20] 运行：02_网络架构\02_DNN前向传播与随机失活.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：5.46 秒
--- 最后 15 行输出 ---
    参数总量：含 Dropout 模型 3490，不含 Dropout 模型 3490  # Dropout 层没有可学习参数
    
    ==========================================================================
    九、本节小结
    ==========================================================================
    1. nn.Linear(in, out) 的 weight 是 (out, in)，前向传播就是 x @ W.T + b（已验证误差为 0）。
    2. 多层网络公式 z(l)=W(l)h(l-1)+b(l)，h(l)=σ(z(l))；没有激活函数时多层线性 ≡ 单层线性（已数值验证）。
    3. 不调 nn.Linear、只用 torch.matmul 手写逐层前向，与 nn.Sequential 结果完全一致（误差 < 1e-6）。
    4. Dropout 训练期随机置零并按 1/(1-p) 缩放（倒置 Dropout），推理期恒等；实测置零比例 ≈ p、训练输出均值 ≈ 输入均值。
    5. eval() 时 Dropout 关闭，必须切换模式，否则评估结果会随机抖动。
    6. 二分类 DNN 训练 10 个 epoch 后，含/不含 Dropout 的 train/test 准确率对比，
       体现了 Dropout 对泛化差距的影响。
    
    脚本总耗时：1.90 秒
    脚本正常结束。

------------------------------------------------------------------------------
[7/20] 运行：02_网络架构\03_CNN卷积与池化.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：3.02 秒
--- 最后 15 行输出 ---
    
    ==========================================================================
    十一、本节小结
    ==========================================================================
    1. 手写双重循环卷积已与 nn.Conv2d 在 5 组 stride/padding 配置下逐元素对齐（误差 < 1e-5）。
    2. 输出尺寸公式 H_out = (H + 2p - kh)/s + 1 是理解 CNN 形状变化的钥匙。
    3. 多通道卷积：输出第 co 个通道 = Σ_ci conv(X[ci], K[co,ci]) + b[co]，偏置只加一次。
    4. 池化无参数：手写 unfold 版最大池化与 nn.MaxPool2d 误差为 0。
    5. AdaptiveAvgPool2d((1,1)) 能把任意 H×W 压成 1×1，是分类头适配多分辨率的常规做法。
    6. 课案 CNN 在 (4,1,28,28) 上输出 (4,10)；参数量 421642，其中 95.5% 集中在
       两个全连接层，卷积层只占 18816 —— 体现权重共享的参数量优势。
    7. 堆叠 3×3 卷积可以便宜地扩大感受野（k 层 -> 感受野 2k+1）。
    
    脚本总耗时：0.61 秒
    脚本正常结束。

------------------------------------------------------------------------------
[8/20] 运行：02_网络架构\04_RNN_LSTM_GRU.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：4.24 秒
--- 最后 15 行输出 ---
                GRU  t=0: 2.68e-11   t=25: 2.13e-06   t=49: 7.55e-01
           三者的 t=0 都比 t=49 小 10 个数量级以上 —— 早期时间步几乎学不动。
           把 W_hh 放大 8 倍使 |λ_max| = 5.71 > 1 后，趋势完全反转：
                t=0 的梯度范数变成 2.17e+05，是 t=49（3.78e-01）的
                5.74e+05 倍 —— 这就是梯度爆炸。
        5. LSTM 用 f_t ⊙ C_{t-1} 开了一条免衰减旁路，且 f_t 可学习。
           实测：遗忘门 bias 从 0（σ≈0.5）抬到 2.0（σ≈0.88）后，t=0 的梯度从
           1.72e-12 提升到 5.68e-03 量级（见上方对照 B 表格），衰减被大幅缓解。
           手写 6 个公式与 nn.LSTM 最大误差 7.45e-08（< 1e-5）。
        6. GRU 合并遗忘门与输入门为更新门，参数是 LSTM 的 3/4；
           手写 4 个公式与 nn.GRU 最大误差 8.94e-08（< 1e-5）。
        7. 小任务实战：LSTM 在「累计和正负判断」上 5 个 epoch 把 loss 从 0.6936
           降到 0.4551，测试准确率 50% → 92.00%。
        8. 本脚本实测总耗时 1.80 秒，图片输出到：
           F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[9/20] 运行：02_网络架构\05_Attention与自注意力.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：2.63 秒
--- 最后 15 行输出 ---
        3. Cross-Attention 的 Q 与 K/V 来自不同序列（seq_q ≠ seq_k）；
           Self-Attention 的 Q/K/V 来自同一序列（权重是 seq×seq 的方阵）。
        4. 自注意力用 W_Q / W_K / W_V 三组不同线性变换把同一份 X 投影成三种角色的向量。
        5. 手写 scaled_dot_product_attention 支持 (batch, heads, seq, d_k)；
           Q,K,V = randn(2,4,5,8) 时 output (2, 4, 5, 8)、weights (2, 4, 5, 5)，
           每行权重和为 1。
        6. mask 用 masked_fill(mask == 0, -1e9)：causal mask（下三角）让位置 i 只看到 ≤ i，
           可验证上三角权重精确为 0；padding mask 让补齐位置权重为 0；
           -1e9 而非 -inf，使「整行全屏蔽」退化成均匀分布而不是 NaN。
        7. 多头：split_heads 走 view → transpose(1,2)；合并走 transpose(1,2) → contiguous() → view()。
           transpose 后 is_contiguous() 为 False，view 会报错，contiguous() 后为 True。
        8. 手写 MultiHeadAttention：W_Q/W_K/W_V/W_O 四个 nn.Linear，d_k = d_model // num_heads；
           d_model=128/heads=4 与 d_model=512/heads=8 两种配置都验证通过。
        9. 本脚本实测总耗时 0.38 秒，图片输出到：
           F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[10/20] 运行：02_网络架构\06_Transformer完整实现.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：4.59 秒
--- 最后 15 行输出 ---
          bool 掩码下 True 表示屏蔽，这个写法把「可见的下三角」全屏蔽了，
          而且最后一行整行被屏蔽 → Softmax 得到 nan。正确写法是
          `torch.triu(torch.ones(L,L), diagonal=1).bool()` 或 float 的
          `nn.Transformer.generate_square_subsequent_mask(L)`。
        · `tgt_is_causal=True` / `src_is_causal=True` 只是**提示**（hint），不是**指令**：
          在 attn_mask=None 时它什么也不做，不会自动加因果掩码（本脚本已实测证明）。
          想要因果性，必须显式传掩码。
        · transpose 之后必须 .contiguous() 才能 view/reshape。
        · 模型内部**不要**做 Softmax：CrossEntropyLoss 自带 log_softmax。
        · 位置编码用 register_buffer，不能用普通属性（否则不随模型保存/迁移设备），
          也不能用 Parameter（它不需要学习）。
        · LayerNorm 用于 NLP 而不是 BatchNorm：与 batch 组成、序列长度无关，
          训练/推理一致，逐 token 归一化。
    
    脚本执行完毕（无任何异常）。

------------------------------------------------------------------------------
[11/20] 运行：03_训练组件\01_激活函数.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：5.60 秒
--- 最后 15 行输出 ---
    relu              0.9047      0.8938           1.622e-02              5.718e-02    0.235303
    
    结论（用真实数值说话）：
      第 1 轮首层梯度范数：Sigmoid=8.289e-04  Tanh=3.841e-02  ReLU=1.622e-02
      -> Sigmoid 的梯度比另外两者小 1~2 个数量级（饱和导致梯度消失），训练起步就慢。
      训练全程平均梯度范数：Sigmoid=9.953e-03 < ReLU=5.718e-02 ≈ Tanh=6.879e-02
      -> Sigmoid 是三者中最小的，梯度始终传不回来，所以它训练最慢、loss 最高。
      末轮 test 准确率：Sigmoid=0.8000，Tanh=0.8687，ReLU=0.8938
      ReLU 正半轴梯度恒为 1、不饱和，梯度最充沛，因此收敛最快、loss 最低 —— 这就是它成为隐藏层首选的原因。
    已保存：F:\ProGram\Python_Base\Deep_Learning\output\03训练组件_01_激活函数训练对比.png
    
    ==============================================================================
    01_激活函数.py 运行完毕
    ==============================================================================
    输出目录：F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[12/20] 运行：03_训练组件\02_损失函数.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：3.72 秒
--- 最后 15 行输出 ---
    CosineEmbedding  1-cos 或 max(0, cos-margin)     度量学习 / 双塔              只看方向，与长度无关
    TripletMargin    max(0, d(a,p)-d(a,n)+m)        人脸识别 / 检索              三元组，拉近正推远负
    Focal Loss       -α(1-p_t)^γ log(p_t)           极端类别不平衡                压低易分样本，聚焦难样本
    
    ==============================================================================
    8. 可视化：回归损失曲线 + 分类损失曲线
    ==============================================================================
    已保存：F:\ProGram\Python_Base\Deep_Learning\output\03训练组件_02_损失函数曲线.png
      左图读数：e=1 时 MSE=1、MAE=1、SmoothL1=0.5（分段点）；e=4 时 MSE=16、MAE=4、SmoothL1=3.5 -> MSE 增长最快。
      右图读数：p_t=0.9 时 BCE=0.1054；p_t=0.1 时 BCE=2.3026 -> 预测越错（p_t 越小），损失以 -log 的速度无上界增长。
    
    ==============================================================================
    02_损失函数.py 运行完毕
    ==============================================================================
    输出目录：F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[13/20] 运行：03_训练组件\03_优化器全家桶.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：7.24 秒
--- 最后 15 行输出 ---
    【补充优化器速查】
    优化器          核心公式                               一句话场景
    ----------------------------------------------------------------------------------------------------
    AdamW        θ ← θ - α[m̂/(√ŝ+ε) + λθ]          解耦权重衰减，W 与 L2 不等价，Transformer 标配
    RAdam        按 ρ_t 判断方差可信度再修正                   自动预热，不用手写 warmup
    SparseAdam   同 Adam，但只更新非零梯度行                   超大稀疏 Embedding 表
    LBFGS        θ ← θ - α·H_k·g（拟牛顿）               小规模全量批，二阶信息，深网络不用
    ASGD         SGD + 参数时间平均                       凸问题理论保证好；现多用 EMA 取代
    Adamax       u=max(β₂u, |g|)，用 ∞-范数             梯度有极端离群值时更稳
    NAdam        Adam 的动量项套 Nesterov 思想             想要 Nesterov 加速 + 自适应
    
    ==============================================================================
    03_优化器全家桶.py 运行完毕
    ==============================================================================
    输出目录：F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[14/20] 运行：03_训练组件\04_评估指标.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：5.64 秒
--- 最后 15 行输出 ---
            [FN=  11, TP=  70]]
         sklearn 混淆矩阵 = [[67, 12], [11, 70]]
         校验：70 + 67 = 137 个判对，共 160 个 → Accuracy = 0.856250
    
      ▶ 完整评估结论（一句话解读）
         Accuracy = 0.8562：整体判对了 85.62% 的样本。
         Precision = 0.8537：模型说是正类的样本里，85.37% 真的是正类。
         Recall = 0.8642：真正的正样本里，86.42% 被找出来了。
         F1 = 0.8589：Precision 与 Recall 的调和平均，两者较均衡。
         AUC = 0.9508：随机取一正一负，正样本分数更高的概率是 95.08%，排序能力良好。
    
    ==============================================================================
    04_评估指标.py 运行完毕
    ==============================================================================
    输出目录：F:\ProGram\Python_Base\Deep_Learning\output

------------------------------------------------------------------------------
[15/20] 运行：03_训练组件\05_参数初始化.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：5.50 秒
--- 最后 15 行输出 ---
      解读：
        · zeros_            ：loss 始终 0.692819 ≈ ln2、准确率 47.00%、梯度范数 0.00e+00 —— 完全学不动（对称性 + 零梯度双重锁死）。
        · normal_(std=1.0)  ：首轮 loss 1912.6，梯度范数 5.175e+03（巨大），训练震荡、最终准确率只有 87.33%。
        · normal_(std=0.01) ：信号逐层衰减，末轮 loss 仅降到 0.6824，收敛极慢。
        · xavier_normal_    ：末轮 loss 0.1845，准确率 94.33%。
        · kaiming_normal_   ：末轮 loss 0.1761，准确率 93.00%。
        → 本实验测试准确率最高的是 xavier_normal_（94.33%）；
          Xavier 与 Kaiming 都稳定收敛到 90% 以上，说明「把方差钉在 1 附近」才是关键，
          具体选哪个由激活函数决定（ReLU 用 Kaiming，Tanh/Sigmoid 用 Xavier）。
    
    已保存：F:\ProGram\Python_Base\Deep_Learning\output\03训练组件_05_初始化训练对比.png
    
    ==============================================================================
    05 参数初始化：全部实验完成
    ==============================================================================

------------------------------------------------------------------------------
[16/20] 运行：03_训练组件\06_正则化.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：13.19 秒
--- 最后 15 行输出 ---
    第 6 部分：正则化方法对比表（课案原表）
    ==============================================================================
    方法                        作用层面            核心机制
    --------------------------------------------------------------------------
    L1 正则                     损失函数            λ·Σ|w|，产生稀疏解
    L2 正则 / Weight Decay      损失 + 优化器        λ·Σw²，限制权重大小
    Dropout                   计算单元            随机丢弃，训练子网络
    BatchNorm                 层级              沿 batch 和特征维归一化
    LayerNorm                 层级              沿特征维归一化
    Early Stopping            训练流程            过拟合前停下
    Data Augmentation         数据              变换扩增样本
    
    ==============================================================================
    06 正则化：全部实验完成
    ==============================================================================

------------------------------------------------------------------------------
[17/20] 运行：03_训练组件\07_学习率调度.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：9.05 秒
--- 最后 15 行输出 ---
    OneCycleLR            每步              先升后降              快速训练
    
    附：另外几个调度器的一句话总结
    调度器                           触发方式              曲线形状              适用场景
    --------------------------------------------------------------------------------------------
    LambdaLR                      每个 epoch          任意函数              完全自定义（多项式/分段/更复杂规则）
    MultiplicativeLR              每个 epoch          逐轮累乘因子            衰减规律随 epoch 变化的场景
    CyclicLR                      每步                三角/指数震荡           跳出局部极小值、配合较大 max_lr
    CosineAnnealingWarmRestarts   每个 epoch          余弦 + 周期重启         SGDR，多次退火重启
    ConstantLR / LinearLR         前 N 步             常数 / 线性           warmup 阶段
    SequentialLR                  按 milestone 切换    多段拼接              warmup + 主调度（工业界最常见）
    
    ==============================================================================
    07 学习率调度：全部实验完成
    ==============================================================================

------------------------------------------------------------------------------
[18/20] 运行：03_训练组件\08_模型保存与推理.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：3.60 秒
--- 最后 15 行输出 ---
    ONNX (.onnx)                      跨框架交换格式                 跨平台兼容                 跨框架部署             转换可能需调试
    TensorFlow (.pb/.ckpt/.h5)        TensorFlow 原生格式         功能全面、生态集成             TensorFlow 生态开发   与 PyTorch 不兼容，需转换
    
      说明：本机只安装了 PyTorch、numpy、scikit-learn、matplotlib、seaborn、pandas、scipy，
            没有 safetensors / onnx / tensorflow —— 所以上表只是**文字讲解**，脚本里不 import 它们。
    
    ==============================================================================
    08 模型保存与推理：全部实验完成
    ==============================================================================
    
    本次运行生成的文件：
      08_checkpoint.pt                             10,935 字节
      08_model_full.pt                              5,337 字节
      08_model_state.pt                             4,195 字节
      03训练组件_08_模型保存与推理.png                        80,598 字节

------------------------------------------------------------------------------
[19/20] 运行：04_训练技巧\01_过拟合与欠拟合演示.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：22.83 秒
--- 最后 15 行输出 ---
    
    【课案对比表：现象 / 训练 loss / 验证 loss / 首选排查】
    +-----------+------------+------------+------------+
    | 现象      | 训练 loss  | 验证 loss  | 首选排查   |
    +-----------+------------+------------+------------+
    | 过拟合    | ↓ 持续降   | ↑ 反弹     | 加正则化   |
    | 欠拟合    | 高且不降   | 高且不降   | 增大模型   |
    | 震荡      | 剧烈波动   | 剧烈波动   | 降低学习率 |
    | 缓慢      | 极慢下降   | 极慢下降   | 调大学习率 |
    | 爆炸/消失 | NaN 或不降 | NaN 或不降 | 梯度裁剪   |
    +-----------+------------+------------+------------+
    
    ==============================================================================
    全部实验完成。图片输出目录：F:\ProGram\Python_Base\Deep_Learning\output
    ==============================================================================

------------------------------------------------------------------------------
[20/20] 运行：04_训练技巧\02_梯度爆炸与梯度消失演示.py
------------------------------------------------------------------------------
返回码：0（成功）  耗时：9.84 秒
--- 最后 15 行输出 ---
    | 缓慢      | 极慢下降   | 极慢下降   | 调大学习率 |
    | 爆炸/消失 | NaN 或不降 | NaN 或不降 | 梯度裁剪   |
    +-----------+------------+------------+------------+
    
    【梯度问题的诊断顺序（本脚本实测支持）】
      1. loss 突然 NaN / inf  → 先看梯度范数：是不是梯度爆炸（本脚本第 4、5 部分）
           → 立刻加 clip_grad_norm_(max_norm=1.0)；再调小学习率、换 Kaiming 初始化、加 BatchNorm。
      2. loss 几乎不降、停在高位，且浅层梯度极小 → 梯度消失（第 2、3 部分）
           → 换 ReLU、改 Kaiming 初始化、加 BatchNorm/残差、别把 Sigmoid 叠太深。
      3. 两者的共同根源都是「跨层连乘的尺度失衡」，所以工程上的标准答案是同一套组合拳：
         **合适的激活 + 匹配的初始化 + 归一化层 + 残差（深网）+ 梯度裁剪（保险）**。
    
    ==============================================================================
    全部实验完成。图片输出目录：F:\ProGram\Python_Base\Deep_Learning\output
    ==============================================================================

==============================================================================
耗时排行（本次实测）
==============================================================================
  [OK  ]  22.83s  04_训练技巧\01_过拟合与欠拟合演示.py
  [OK  ]  13.19s  03_训练组件\06_正则化.py
  [OK  ]  10.00s  01_PyTorch基础\03_数据集与DataLoader.py
  [OK  ]   9.84s  04_训练技巧\02_梯度爆炸与梯度消失演示.py
  [OK  ]   9.05s  03_训练组件\07_学习率调度.py
  [OK  ]   7.24s  03_训练组件\03_优化器全家桶.py
  [OK  ]   5.64s  03_训练组件\04_评估指标.py
  [OK  ]   5.60s  03_训练组件\01_激活函数.py
  [OK  ]   5.50s  03_训练组件\05_参数初始化.py
  [OK  ]   5.46s  02_网络架构\02_DNN前向传播与随机失活.py
  [OK  ]   4.59s  02_网络架构\06_Transformer完整实现.py
  [OK  ]   4.24s  02_网络架构\04_RNN_LSTM_GRU.py
  [OK  ]   4.20s  01_PyTorch基础\04_权重存储与精度类型.py
  [OK  ]   3.83s  02_网络架构\01_Embedding词嵌入.py
  [OK  ]   3.72s  03_训练组件\02_损失函数.py
  [OK  ]   3.60s  03_训练组件\08_模型保存与推理.py
  [OK  ]   3.36s  01_PyTorch基础\02_自动微分.py
  [OK  ]   3.02s  02_网络架构\03_CNN卷积与池化.py
  [OK  ]   2.63s  02_网络架构\05_Attention与自注意力.py
  [OK  ]   2.57s  01_PyTorch基础\01_张量.py
  合计耗时：130.12 秒

==============================================================================
共 20 个脚本，成功 20 个，失败 0 个
==============================================================================

```
